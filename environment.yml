name: cameratraps-env
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - cudatoolkit=11.3
  - cudnn=8.1
  - tqdm
  - pip
  - python=3.9
  - conda-build
  - pytorch::pytorch=1.10.1
  - pytorch::torchvision=0.11.2
  - pip:
      - customtkinter==5.2.0
      - exifread==3.0.0
      - notebook
      - pyexiftool
      - pyfastcopy
      - tensorflow==2.9
      - gdown
      - notebook
      - pandas
      - megadetector
